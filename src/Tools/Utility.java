package Tools;
import App;
public class Utility {
    
    public static Character searchForCharacter(int targetX, int targetY) {
        for (int spot = 0; spot <  ; spot++ ) {
            
        }
    }
    public static Boolean searchForWord(int startX, int startY, String Word, int direction) {
        for (int whereAmI = 1; whereAmI < Word.length(); whereAmI++) {
            int wordPosition = 1;
            if (direction == 0) { //Up left check
                
            }
            if (direction == 1) { //Up check

            }
            if (direction == 2) { //Up right check

            }
            if (direction == 3) { //Right check

            }
            if (direction == 4) { //Down right check

            }
            if (direction == 5) { //Down check

            }
            if (direction == 6) { //Down left check

            }
            if (direction == 7) { //Left check

            }
        }
    }
}
